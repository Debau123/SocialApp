/** @type {import('next').NextConfig} */
const nextConfig = {
    images:{
        domains: [
            'p6twf6qi3thuid9j.public.blob.vercel-storage.com',
              's.gravatar.com',
            'lh3.googleusercontent.com'

        ]
    }
};

export default nextConfig;